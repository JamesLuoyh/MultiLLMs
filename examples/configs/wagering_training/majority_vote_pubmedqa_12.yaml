# Evaluation configuration for equal wagers on PubMedQA
# No training is necessary for equal wagers

# Include model configs (must match training)
_include_models:
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml
  - models/llama3_aloe_8b_alpha.yaml

# Checkpoint path (for equal_wagers, this can be a dummy path since no training is needed)
checkpoint_path: /common/users/yl2310/MultiLLMs/checkpoints/dummy_equal_wagers

# Apply adaptive temperature scaling to cached option logits before wagering.
calibrated: false
_include_calibration: calibration/adaptive_temperature_pubmedqa_1000samples.yaml

shuffle_data: true  # Shuffle training data across datasets
shuffle_seed: 18 #18 #17

# Wagering method - equal wagers (no trainable parameters)
wagering_method:
  name: equal_wagers
  config: {}

# Aggregation function (alternative method)
aggregation:
  name: majority_vote
  config: {}

# ============================================================================
# TRAINING CONFIGURATION
# ============================================================================

# # Training datasets
# _include_datasets:
#   - datasets/mmlu.yaml
#   - datasets/medmcqa.yaml

# # Override dataset-specific settings for training
# datasets:
#   - train_split: train
#   - train_split: train

# # Training parameters
# num_epochs: 1
# checkpoint_base_dir: /common/users/yl2310/MultiLLMs/checkpoints
# save_every: 100
# training_batch_size: 100

# # Training options
# auto_resume: false
# shuffle_data: true
# shuffle_seed: 18 #18 #42
# early_stopping_patience: 10
# validation_split_ratio: 0.1

# Wandb logging
report_to_wandb: true
wandb_project: multi-llm-wagering
wandb_entity: null
wandb_name: equal_wagers_pubmedqa_eval_majority_vote

# Include test dataset configs
_include_test_datasets:
  - datasets/pubmedqa_context_only.yaml

# Override dataset-specific settings for evaluation
test_datasets:
  - display_name: pubmedqa_test

# Evaluation parameters
option_tokens: ["YES", "NO"]

# Evaluation checkpoint/resume (set to false to start fresh)
resume_eval: false

# Paths
cache_path: /common/users/yl2310/MultiLLMs/cache